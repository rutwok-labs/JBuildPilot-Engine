<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
        <artifactId>jbuildpilot-parent</artifactId>
        <version>0.1.0-alpha.1</version>
    </parent>

    <artifactId>jbuildpilot-cli</artifactId>

    <name>JBuildPilot Engine - CLI</name>
    <description>Command Line Interface for the JBuildPilot engine.</description>

    <dependencies>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-template</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-tasks</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-orchestrator</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-analyzer</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-resolver</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-downloader</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-security</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-builder</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-environment</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.rutwok-labs.jbuildpilot</groupId>
            <artifactId>jbuildpilot-github</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>jbuildpilot-cli</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>io.github.rutwoklabs.jbuildpilot.cli.JBuildPilotCli</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
